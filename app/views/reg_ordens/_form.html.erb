<%= form_with(model: reg_orden, local: true) do |form| %>
  <% if reg_orden.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reg_orden.errors.count, "error") %> prohibited this reg_orden from being saved:</h2>

      <ul>
      <% reg_orden.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cliente_id %>
    <%= form.text_field :cliente_id %>
  </div>

  <div class="field">
    <%= form.label :fecha %>
    <%= form.date_select :fecha %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
